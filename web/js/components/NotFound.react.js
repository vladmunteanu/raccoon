import React from 'react';


let NotFound = React.createClass({

    /**
     * @return {object}
     */
    render: function () {
        return (
            <h1>Page not found</h1>
        );
    },

});

export default NotFound;
